import { FormValidationInterceptor } from './form-validation.interceptor';

describe('FormValidationInterceptor', () => {
  it('should be defined', () => {
    expect(new FormValidationInterceptor()).toBeDefined();
  });
});
