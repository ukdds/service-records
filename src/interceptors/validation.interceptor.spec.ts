import { ValidationInterceptor } from './validation.interceptor';

describe('ValidationInterceptor', () => {
  it('should be defined', () => {
    expect(new ValidationInterceptor()).toBeDefined();
  });
});
